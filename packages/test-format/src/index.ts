/**
 * The `qabrain/test/v1` intent-level test format: zod schema, canonicalisation and the resolved-cache key.
 *
 * Reserved for milestone M1 — the format is specified in `docs/test-format.md`, and the gateway exposes
 * `qa_test_save` / `qa_test_get` / `qa_test_list` as stubs so their schemas are reviewable today.
 */
export {};
