/**
 * Self-healing primitives: failure classification, the T0 deterministic locator chain, Similo++-style
 * weighted similarity scoring, and the pHash visual tie-break.
 *
 * Reserved for milestone M3 — the algorithm, thresholds and guardrails are specified in
 * `docs/self-healing.md`, and the gateway already exposes `qa_heal_locator` as a stub so the tool schema
 * is reviewable today. Nothing is exported yet on purpose: an empty module is honest about that.
 */
export {};
