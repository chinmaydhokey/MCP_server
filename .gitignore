# secrets — never commit
.env
.env.*
!.env.example
!deploy/.env.example
# Docker secret files mounted by deploy/docker-compose*.yml (QA_BRAIN_SECRETS_DIR).
# Better still: point QA_BRAIN_SECRETS_DIR outside the working tree entirely.
deploy/secrets/

# build & deps
node_modules/
dist/
*.tsbuildinfo
.turbo/

# runtime state
.qa-brain/
test-results/
playwright-report/
coverage/

# editors / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
