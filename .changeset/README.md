# Changesets

Run `pnpm changeset` to record a user-facing change. All publishable packages are version-locked
(`fixed` group), so one changeset bumps the whole toolchain together. Publishing is disabled in M0.
